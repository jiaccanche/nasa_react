import { Box, Button, IconButton, AppBar, Toolbar } from '@material-ui/core';
import React from 'react';
import MenuIcon from "@material-ui/icons/Menu";

const Navbar = () => {
  return(
    <AppBar>
        <Toolbar></Toolbar>
    </AppBar>    
  );
}

export default Navbar;